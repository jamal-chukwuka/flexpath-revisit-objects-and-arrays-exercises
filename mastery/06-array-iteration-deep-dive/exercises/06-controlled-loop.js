/**
 * Inspect readings from left to right and return the first valid numeric reading
 * greater than limit.
 *
 * Ignore null, undefined, and non-number entries.
 * Stop examining the array immediately once a qualifying reading is found.
 * Return undefined if none qualifies.
 *
 * This exercise intentionally targets loop control. Use a loop that supports
 * continue and break/early exit; do not use find/filter/map/forEach/reduce.
 */
export default function firstValidReadingAbove(readings, limit) {
  // Write your solution here.
}
