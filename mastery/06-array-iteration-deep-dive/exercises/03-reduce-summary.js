/**
 * Return the total billable amount for completed work.
 * Each record has { hours, rate, completed }.
 * Only completed records contribute hours * rate.
 *
 * The empty array must return 0.
 * Do not mutate records.
 */
export default function completedBillableTotal(records) {
  // Write your solution here.
}
