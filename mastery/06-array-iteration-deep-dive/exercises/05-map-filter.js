/**
 * Return an array of usernames for ACTIVE users whose role equals targetRole.
 * Preserve input order.
 * Do not mutate users or return references to a modified input array.
 */
export default function activeUsernamesByRole(users, targetRole) {
  // Write your solution here.
}
