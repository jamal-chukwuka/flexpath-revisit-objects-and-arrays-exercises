# Module 6 — Array Iteration Deep Dive Mastery

This checkpoint covers only Matt Henry's **Array Iteration Deep Dive** module.

## Rules
1. Work cold. Do not open `resources.md` until you have committed your first attempt.
2. Answer the conceptual prompts in `conceptual.md` in your own words.
3. Implement the coding challenges in `exercises/`.
4. Run the unit tests.
5. Commit and push the untouched cold result before remediation.

## Run the coding tests

```bash
node mastery/06-array-iteration-deep-dive/tests/run-tests.js
```

The tests check behavior, edge cases, mutation, early termination, and method semantics. They do not require one exact implementation unless the exercise explicitly asks for a control-flow property.

After the cold push, use `resources.md` only for concepts demonstrated as weak.