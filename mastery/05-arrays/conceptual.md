# Section 5 — Arrays Conceptual Retrieval

Complete this **cold**. Do not consult the course, MDN, Mochi, AI, or your earlier `mastery-drill.js` answers. If unsure, write **not sure**.

1. What is the difference between assigning an array to another variable and creating a shallow copy?
2. If two variables refer to the same array, what should `a === b` evaluate to? Why?
3. What does a shallow copy duplicate? What can still be shared when an array contains objects or other arrays?
4. What must the callback passed to `find()` communicate? When does the search stop?
5. Contrast `find()`, `filter()`, `map()`, and `forEach()` by the job each performs.
6. What does `forEach()` itself return?
7. Name three common array operations that mutate the original array.

Do not correct these answers after running the coding tests. Push the first attempt as evidence.
