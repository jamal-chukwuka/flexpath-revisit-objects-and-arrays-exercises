/**
 * EXTRA PRACTICE — Shallow-copy behavior
 *
 * Return a new outer array containing the same task objects as tasks,
 * but in reverse order.
 *
 * CONSTRAINTS:
 * - Do NOT change the order of the original tasks array.
 * - The returned outer array must be a different array.
 * - The task objects themselves must remain the same object references.
 * - Keep the function name, parameter, and export unchanged.
 * - Choose the operations yourself.
 */
function reversedTaskView(tasks) {
  // Write your solution here.
}
export default reversedTaskView;
