/**
 * TASK: Return a shallow copy of roster.
 *
 * STARTING POINT:
 * - `roster` is the array you are given. The test runner supplies it.
 * - Do NOT create your own starter roster or hard-code objects.
 *
 * REQUIRED RESULT:
 * - The returned outer array must be a different array from `roster`.
 * - Nested objects should still be the same referenced objects.
 * - Do not mutate `roster`.
 *
 * Keep the function name, parameter, and export unchanged.
 * Decide for yourself how to create the shallow copy.
 */
function copyRoster(roster) {
  // Write your solution here.
}
export default copyRoster;
