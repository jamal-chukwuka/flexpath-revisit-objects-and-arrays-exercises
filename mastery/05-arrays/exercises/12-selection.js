/**
 * EXTRA PRACTICE — Choose the operation from the required behavior.
 *
 * You are given customer records.
 *
 * Return the FIRST customer who:
 * - has an active account, AND
 * - has credits greater than or equal to minimumCredits.
 *
 * Return undefined when nobody qualifies.
 * Do not mutate customers.
 *
 * The prompt intentionally does not tell you which array method to use.
 */
function qualifyingCustomer(customers, minimumCredits) {
  // Write your solution here.
}
export default qualifyingCustomer;
