/**
 * EXTRA PRACTICE — Applied side effects
 *
 * For every notification, call send(notification.id, notification.message)
 * exactly once and in the original order.
 *
 * INPUT:
 * - notifications is supplied by the caller.
 * - send is a supplied function.
 *
 * CONSTRAINTS:
 * - Do not create a transformed result array.
 * - Do not mutate notifications.
 * - The function should not return a collection.
 * - Keep the function name, parameters, and export unchanged.
 * - Choose the iteration approach yourself.
 */
function sendNotifications(notifications, send) {
  // Write your solution here.
}
export default sendNotifications;
