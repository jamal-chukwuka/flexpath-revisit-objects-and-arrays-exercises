/**
 * EXTRA PRACTICE — Choose the operation from the required behavior.
 *
 * Return a NEW array containing the email address of EVERY member whose:
 * - role equals requiredRole, AND
 * - active property is true.
 *
 * Preserve order and do not mutate members.
 *
 * The prompt intentionally does not tell you which operation or
 * combination of operations to use.
 */
function activeMemberEmails(members, requiredRole) {
  // Write your solution here.
}
export default activeMemberEmails;
