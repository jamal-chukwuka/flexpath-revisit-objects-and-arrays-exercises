/**
 * EXTRA PRACTICE — Applied lookup
 *
 * Return the FIRST ticket assigned to ownerId that is still unresolved.
 *
 * INPUT:
 * - tickets is an array of ticket objects.
 * - ownerId identifies the owner you are looking for.
 * - A ticket is unresolved when resolved is false.
 *
 * CONSTRAINTS:
 * - Do not mutate tickets.
 * - Return undefined when no ticket satisfies both conditions.
 * - Keep the function name, parameters, and export unchanged.
 * - Choose the array operation yourself.
 */
function firstOpenTicketForOwner(tickets, ownerId) {
  // Write your solution here.
}
export default firstOpenTicketForOwner;
