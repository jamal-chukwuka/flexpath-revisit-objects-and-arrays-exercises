/**
 * EXTRA PRACTICE — Choose the operation from the required behavior.
 *
 * For each order, call report(order.id) when order.flagged is true.
 * Calls must occur in the same order as the input.
 *
 * Do not mutate orders. Do not return a transformed collection.
 *
 * The prompt intentionally does not name the intended array method.
 */
function reportFlaggedOrders(orders, report) {
  // Write your solution here.
}
export default reportFlaggedOrders;
