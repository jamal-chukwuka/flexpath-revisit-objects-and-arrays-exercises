/**
 * TASK: Return a NEW array containing only the name from each user.
 * Preserve the original order.
 *
 * STARTING POINT:
 * - `users` is the array you are given. The test runner supplies it.
 * - Each element provides the data needed for the output.
 * - Do NOT create your own starter array or hard-code users.
 *
 * CONSTRAINTS:
 * - Produce one output value for each input element.
 * - Do not mutate `users`.
 * - Keep the function name, parameters, and export unchanged.
 * - Decide for yourself how to perform the transformation.
 */
function userNames(users) {
  // Write your solution here.
}
export default userNames;
