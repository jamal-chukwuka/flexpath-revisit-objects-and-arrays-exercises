/**
 * TASK: Return the FIRST user whose id equals targetId.
 * If no user matches, return undefined.
 *
 * STARTING POINT:
 * - `users` is the array you are given. The test runner supplies it.
 * - `targetId` is the id you are searching for.
 * - Do NOT create your own starter array or hard-code users.
 *
 * CONSTRAINTS:
 * - Do not change the supplied array.
 * - Keep the function name, parameters, and export unchanged.
 * - Decide for yourself how to search the array.
 */
function findUserById(users, targetId) {
  // Write your solution here.
}
export default findUserById;
