/**
 * EXTRA PRACTICE — Applied selection
 *
 * Return a NEW array containing products that:
 * - are in stock, AND
 * - have a price less than or equal to maxPrice.
 *
 * INPUT:
 * - products is an array of product objects.
 * - maxPrice is the highest allowed price.
 *
 * CONSTRAINTS:
 * - Preserve the original order.
 * - Do not mutate products.
 * - Keep the function name, parameters, and export unchanged.
 * - Choose the array operation yourself.
 */
function affordableInStock(products, maxPrice) {
  // Write your solution here.
}
export default affordableInStock;
