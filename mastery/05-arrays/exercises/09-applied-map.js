/**
 * EXTRA PRACTICE — Applied transformation
 *
 * Return a NEW array of display objects shaped like:
 * { id, label }
 *
 * For each employee:
 * - id comes from employee.id
 * - label should contain the employee's name and department in this format:
 *   "NAME — DEPARTMENT"
 *
 * CONSTRAINTS:
 * - Produce exactly one display object per employee.
 * - Preserve order.
 * - Do not mutate employees or their nested objects.
 * - Keep the function name, parameter, and export unchanged.
 * - Choose the array operation yourself.
 */
function employeeLabels(employees) {
  // Write your solution here.
}
export default employeeLabels;
