# Objects and Properties — Mastery Check

Source: Pluralsight, Matt Henry, *Revisiting Core Concepts in JavaScript: Objects and Arrays*.

This checkpoint follows the **Objects and Properties** section. Do not use the transcript, slides, Google, or execute the prediction snippet until the cold portion is complete.

## Round 1 — Cold Retrieval

Write answers in `responses.md`.

1. In your own words, what is a JavaScript object?
2. Given:

```js
const user = {
  username: "jamal",
  timezone: "America/Chicago"
};
```

Identify the properties, property names/keys, and property values.

3. What do you currently understand a JavaScript prototype to be? If the model is fuzzy, describe the fuzzy model rather than looking it up.

## Game Within the Game — Prediction

Without executing this code first:

```js
const user = {
  name: "Jamal",
  role: "engineer"
};

user.location = "Chicago";

console.log(user.location);
console.log(user.language);
```

Predict exactly what each `console.log` produces and explain why.

## Submission

Complete `responses.md` and commit it. The next challenge is determined from the evidence in that attempt rather than being exposed in advance.

## Evidence Rules

- Cold means closed-reference.
- Uncertainty is evidence; do not hide it.
- Do not optimize for polished prose.
- No score for merely completing the section.
- Evidence is based on retrieval, prediction, production, diagnosis, and later transfer.
